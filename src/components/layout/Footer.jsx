import React from "react";

const Footer = () => {
	return (
		<footer className='footer p-8 bg-gray-500 text-primary-content footer-center'>
			<p>Github Finder {new Date().getFullYear()} &copy; </p>
		</footer>
	);
};

export default Footer;
